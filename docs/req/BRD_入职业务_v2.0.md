# 业务需求文档（BRD）— 入职业务数字化平台

| 项 | 内容 |
|----|------|
| 文档版本 | v2.0（详细版） |
| 创建日期 | 2026-05-30 |
| 适用范围 | F020 入职登记、F060 二维码签到、F090 满意度评价 |
| 上游需求 | 需求规格说明书_入职业务_v1.0.md |

> 说明：本文档面向开发团队与业务干系人，包含完整字段定义、业务规则、接口契约、异常处理与验收标准。涉及工单字段的部分，apiName 以 ServiceGo `GET /api/v1/fields` 返回为准，本文档列出的为业务约定值。

---

## 1. 项目背景与目标

### 1.1 项目目标

| 序号 | 目标     | 对应能力                                        |
|------|--------|---------------------------------------------|
| G1 | 移动端自助化 | H5 端完成登录、隐私签署、指引查阅、资料填写、承诺签署、签到、评价       |
| G5 | 独立页面功能 | 签到、满意度评价                                  |

> 注：G 编号对应上游《需求规格说明书_入职业务_v1.0》的目标项，本期仅涉及 G1、G5，故编号不连续。

### 1.2 范围说明

**本期（in scope）：**
- F020 入职登记全部 8 个子功能
- F060 二维码签到、F090 满意度评价
- AI 审核仅覆盖**身份证、教育信息、银行卡 图片识别 + 字段比对**

---

## 2. 业务流程

### 2.1 端到端主流程

```
②移动端登录(RS020101)  手机号+验证码，校验工单状态
  ▼
③隐私声明签署(RS020102)  调用DPO，未签不可继续
  ▼
④入职指引查阅(RS020103)  按基地/用工形式动态展示
  ▼
⑤入职资料填写(RS020104)  122字段 + 9个附件字段，暂存/提交
  ▼
⑥真实性承诺签署(RS020105)  H5页面承诺书，点击勾选确认，并提交后不可撤销
  ▼
⑦AI审核(RS020107)  异步Dify身份证识别+比对。并将审核结果同步到入职工单
  ▼
⑨二维码签到(RS060101)  独立页面。扫描现场二维码完成入职签到，系统更新签到时间与签到状态至对应入职工单。
  ▼
⑫入职完成 ──► 满意度评价(RS090102)  独立页面。用户通过点击邮箱链接，对入职服务进行满意度评价，评价结果自动同步至对应入职工单。
```

任意节点可触发 **入职终止**，终止后工单状态置「入职终止」，员工登录被拒。

### 2.2 工单状态字段（orderStatus）

**核心权限规则（来自需求 RS020101）：**
- 工单不存在 或 状态 = `入职终止` → **拒绝登录**
- 状态 = `入职登记` → 字段**可编辑**
- 其余状态 → 字段**只读**

---

## 3. 功能需求详述

> 每个功能包含：功能描述、数据项（字段级）、业务规则、接口、异常处理、验收标准。

### 3.1 RS020101 移动端登录

**功能描述**：待入职员工点击短信/邮件中的链接，通过「手机号 + 验证码」一键登录移动端。

**数据项**：

| 数据项 | 类型 | 长度 | 必填 | 来源 | 说明 |
|--------|------|------|------|------|------|
| 手机号码 | 文本 | 11 | 是 | 用户输入 | 验证码发送与身份识别 |
| 验证码 | 文本 | 6 | 是 | 系统随机生成，存 Redis，短信下发 | 有效期 5 分钟 |
| 工单状态 | 枚举 | — | 是 | entryWorkOrder.orderStatus | 决定进入页面及编辑权限 |

**业务规则**：
1. 输入手机号 → 调用「按手机号查工单」接口（POST ServiceGo `/api/v1/datas/search`，参考 `EntryWorkOrderSearchByPhoneAndStatusTest`；`filterId = 241586`，条件 `personalPhone is_any` + `orderStatus not "入职终止"`），返回为空 → 提示「未找到入职信息，请联系 HR」，**不下发验证码**。
2. 工单状态 = `入职终止` → 提示「入职流程已终止」，拒绝登录。
3. 校验通过 → 生成 6 位数字验证码存 Redis（key 含手机号，TTL 300s），调用**短信平台**下发（见下方「短信平台说明」）。
4. 同一手机号 60 秒内不可重复获取验证码；验证码错误累计 5 次锁定 2 小时。
5. 验证成功 → 按工单状态决定后续页面可编辑性。
6. 登录后落地页：未签隐私声明 → 隐私声明页；已签隐私声明 → 入职流程主页。

**关联接口**：I020-01-01（工单查询 + 短信记录保存）。

**异常处理**：
- 短信发送失败 → 自动重试 3 次，仍失败提示「验证码发送失败，请稍后重试」。

**验收标准**：
- 终止工单/不存在工单无法获取验证码。
- 验证码超 5 分钟失效；错误 5 次锁定2个小时，由开发人员自己手动测试验收。
- 非「入职登记」状态登录后所有字段只读。

> **短信平台说明**：本文所称「短信平台」即赛力斯短信网关，对应后端 `SmsService`（`com.example.sms.service.SmsService`），冒烟测试见 `SmsServiceTest#testSendSms`。配置前缀 `sms.api`（见 `application.properties`）：
>
> | 项 | 值 | 说明 |
> |----|----|------|
> | 取 token 接口 | `POST https://sms-test.seres.cn/auth/accesstoken` | header 传 `appkey` / `appsecret`，返回 `token` + `expire`；服务内缓存，提前 300s 过期续期 |
> | 发短信接口 | `POST https://sms-test.seres.cn/api/sms/messages` | header 传 `token`，body 含 `consumer` + `list[{phone, templateId}]`；`code=0` 为成功 |
> | appKey / consumer | `853432` / `50` | 测试环境配置，上线前替换为生产值 |
> | 验证码模板 ID | 待配置 | `SmsServiceTest` 中样例模板为 `00037625`；登录验证码模板以实际申请为准 |
>
> 注：以上为**测试环境**地址与密钥，生产环境需在 `application.properties`（或 yml）覆盖；`appSecret` 等配置直接写在 yml 中并随仓库提交即可。

---

### 3.2 RS020102 隐私声明签署

**功能描述**：调用 DPO 隐私协议服务展示协议内容，员工签署后记录确认状态并回写工单。

**前置条件**：已登录成功。

**数据项**：

| 数据项 | 类型 | 必填 | 来源 | 说明 |
|--------|------|------|------|------|
| 隐私声明内容 | HTML | 是 | DPO 服务接口 | DPO 提供网页/接口，本系统仅展示 |
| 确认状态 | 布尔 | 是 | 员工操作 | 已确认 / 未确认 |
| 确认时间 | 时间戳 | 是 | 系统生成 | 签署成功时间 |
| 隐私声明确认 | 列表（工单任务） | 是 | 工单系统 | 签署后置「完成」 |

**业务规则**：
1. 进入页面调用 DPO 接口获取协议 HTML 内容并渲染。DPO 侧接口暂未提供，本期后端先以 Mock 实现，待对方接口就绪后替换为真实调用。
2. 员工阅读后点击「同意并签署」→ 写工单「隐私声明确认 = 完成」+ 确认时间。
3. 已签署再次进入 → 展示「已签署」态，禁止重复签署。
4. **未签署不允许进入入职指引/资料填写环节**（前端路由 + 后端双重拦截）。
5. 需求要求：**分析哪些字段动态展示与更新** —— 隐私声明的展示内容来自 DPO（动态），回写工单的字段为「确认状态、确认时间、隐私声明确认任务」三项。

**关联接口**：I020-02-01（DPO 取内容 + 确认状态写工单），DPO 侧开发中，后端先以 Mock 实现。

**异常处理**：DPO 接口不可用 → 展示「隐私协议加载失败，请稍后重试」，签署按钮置灰，不可绕过。

**验收标准**：未签署无法进入下一步；签署状态与时间正确回写工单；重复进入不可二次签署。

---

### 3.3 RS020103 入职指引查阅

**功能描述**：按基地与用工形式动态展示入职指引（报到地点与交通、所需材料、注意事项等）。

**前置条件**：已签署隐私声明。

**数据项**：

| 数据项 | 类型 | 必填 | 来源 | 说明 |
|--------|------|------|------|------|
| 报到地点与交通 | 富文本 | 是 | 配置表/外部接口 | 按报到地点动态匹配 |
| 阅读确认状态 | 布尔 | 否 | 员工操作 | 已阅读 / 未阅读 |
| 阅读时间 | 时间戳 | 否 | 系统生成 | |
| 入职指引查阅 | 列表（工单任务） | 否 | 工单系统 | 阅读后置「完成」 |

**业务规则**：
1. 读取工单的「报到地点 / 用工形式」字段，调用对方提供的入职指引接口取内容。该接口暂未提供，后端先以 Mock 实现。
2. 页面实现参考原型 `F:\Jay\h5-app\guide.html`，**所有字段从后端接口获取，不得硬编码**（原型示例数据见 3.3.1）。
3. 进入页面即更新工单「入职指引查阅」相关字段（阅读状态 + 阅读时间）。
4. 动态展示分析：报到地点与交通等内容为动态（按基地/用工形式），回写字段为阅读状态、阅读时间、查阅任务。

#### 3.3.1 原型页面结构（guide.html）

页面为移动端首页，含顶部欢迎卡 + 4 个分区 + 底部 4 Tab（首页 / 办理 / 客服 / 我的）。所有示例值最终由后端接口提供。

**① 欢迎与状态卡**：员工姓名、员工编号、当前办理状态、预约基地、入职日期、所属部门。
说明：这些字段取值工单系统接口
> 示例：张三 / SLS20260520001 / 待入职登记 / 五云湖基地 / 2026年05月20日 / 智能驾驶部

**② 入职流程（进度条）**
说明：查询工单的入职工单字段接口，optionList有两个item，name为"完成"，"未完成"的，与当前业务流程有关的字段列表


		{
			"id": 758888,
			"label": "入职指引查阅",
			"apiName": "onboardGuideRead",
			"fieldTypeName": "选项列表",
			"permissionCode": 4,
			"optionList": [
				{
					"id": 1,
					"name": "完成",
					"iconUrl": "/backend/storage/resource?req=36933159&t=1778900586000"
				},
				{
					"id": 2,
					"name": "未完成",
					"iconUrl": "/backend/storage/resource?req=36933165&t=1778900597000"
				}
			]
		},

**③ 所需材料清单（携带原件提示）**
说明：该模块固定显示下面的信息

| 材料 | 说明                |
|------|-------------------|
| 身份证原件 | 中国大陆居民二代身份证       |
| 学历证书原件 | 最高学历毕业证、学位证       |
| 银行卡 | 本人名下借记卡，用于发放工资    |
| 离职证明 | 上一家单位开具的离职证明原件    |
| 一寸照片 | 红底白衬衫，近期免冠照片 2 张  |
| 体检报告 | 近 3 个月内二级甲等以上医院出具 |

**④ 报到地点与交通（Tab 切换，按基地动态展示）**
说明：调用对方提供的入职指引接口取内容。该接口暂未提供，后端先以 Mock 实现

| 基地 | 地址 |
|------|------|
| 五云湖基地 HRSSC 服务中心 | 重庆市渝北区五云湖大道 188 号赛力斯汽车总部 A 栋 1 楼 |
| 礼嘉基地入职办理中心 | 重庆市渝北区礼嘉街道礼贤路 66 号赛力斯礼嘉基地 B 栋 2 楼 |

**⑤ 注意事项**
说明：该模块固定显示下面的信息

- 入职当日 **08:30-09:30** 到达办理地点，携带所有所需材料原件
- 建议提前 1 天安装「飞书」APP，入职当天需飞书扫码签到（页面含"飞书安装引导"入口 → im-guide.html）
- 着装：商务休闲，建议带外套（空调区域温度较低）
- 疑问可点底部「客服」Tab 咨询在线客服，或拨打 HRSSC 热线 **400-888-2026**
- 入职前务必完成线上信息登记，否则影响当日办理进度
- 底部「开始入职登记」按钮 → 跳转 info-submit.html

**验收标准**：不同基地/用工形式展示差异化内容；阅读状态正确回写。

---

### 3.4 RS020104 提交入职资料（核心）

**功能描述**：员工分组填写个人信息并上传证明材料，支持「录入 / 暂存 / 提交」三种模式。共 13 个信息类型、122 个业务字段、9 个附件字段（身份证正反面共用 `validIdCard` 多图字段）。

**前置条件**：已签署隐私声明；工单状态 = `入职登记`（否则只读）。

**字段控件映射规则**（依据 `GET /api/v1/fields`）：

| fieldTypeName | 前端控件 | 取值/校验 |
|---------------|----------|-----------|
| 选择列表 | 下拉框 | 选项取 `optionList[].name`，按 `optionList[].id` 升序；传值传 name |
| 单行文本 / 自定义 | 文本框 | maxLength 按字段 |
| 数字 | 数字框 | |
| 日期 | 日期选择器 | `yyyy-MM-dd` |
| 日期时间 | 日期时间选择器 | |
| 电话 / 邮箱 | 文本框 + 格式校验 | |
| 文件字段 | 文件上传 | **走附件代理接口，不走 fieldDataList（见 3.4.2）** |
| 员工查找 | 员工搜索组件 | |

前端进页面调用一次 fields 接口，按 apiName 建 Map 缓存复用，缓存不能放到后端，否则对方接口返回值变动的话需要重启后端服务。

#### 3.4.1 信息分组与字段清单

**① 基本信息**

| API字段名 | 名称 | 类型 | 必填 | 备注 |
|-----------|------|------|------|------|
| chineseName | 中文全名 | 单行文本 | 是 | 须与身份证一致 |
| foreignName | 外文全名 | 单行文本 | 是 | 中文名自动带拼音，可改 |
| personalPhone | 个人电话 | 电话 | 是 | |
| personalEmail | 个人邮箱 | 邮箱 | 是 | |
| nationality | 国籍 | 单选 | 是 | =中国时 民族/籍贯 必填 |
| gender | 性别 | 单选 | 是 | |
| birthDate | 出生日期 | 日期 | 是 | 须与身份证一致 |
| maritalStatus | 婚姻状况 | 单选 | 是 | =已婚 展示结婚日期 |
| marryDate | 结婚日期 | 日期 | 否 | 婚姻=已婚 条件展示 |
| nation | 民族 | 单选 | 条件 | 国籍=中国 必填 |
| nativePlace | 籍贯 | 单选 | 条件 | 国籍=中国 必填 |
| householdType | 户口性质 | 单选 | 是 | |
| householdAddress | 户口所在地 | 单选 | 是 | 须与身份证一致 |
| politicalStatus | 政治面貌 | 单选 | 是 | =中共党员 展示入党时间 |
| partyJoinDate | 入党时间 | 日期 | 条件 | 政治面貌=党员 条件展示 |

**② 个人信息（扩展）**

| API字段名 | 名称 | 类型 | 必填 |
|-----------|------|------|------|
| workStartDate | 参加工作日期 | 日期 | 是 |
| drivingLicenseType | 驾照类型 | 单选 | 是 |
| healthStatus | 健康状况 | 单选 | 是 |
| unhealthyExplain | 不健康说明 | 单行文本 | 否 |
| height | 身高 | 数字 | 否 |
| weight | 体重 | 数字 | 否 |

**③ 地址信息**

| API字段名 | 名称 | 类型 | 必填 | 备注 |
|-----------|------|------|------|------|
| homeAddress | 家庭地址 | 单行文本 | 是 | 省/市/区下拉 + 街道手输 |
| postalCode | 邮政编码 | 数字 | 是 | |

**④ 银行账号**

| API字段名 | 名称 | 类型 | 必填 | 备注 |
|-----------|------|------|------|------|
| bankAccount | 银行账号 | 数字 | 否 | 按卡号自动带出发薪银行 |
| salaryBank | 发薪银行 | 单选 | 否 | 系统自动带出 |

**⑤ 其他信息（合规声明类，均必填单选）**

| API字段名 | 名称 |
|-----------|------|
| outerWorkAgeMonth | 外单位累计工龄（月，数字，必填，默认0） |
| isVeteran | 是否复转或退伍军人 |
| isDisabled | 是否残疾人 |
| isCriminalRecord | 是否有犯罪记录 |
| isDrugHistory | 是否吸毒史 |
| isSeriousDisease | 是否有慢性病或重大病史 |
| isCompeteAgreement | 是否签订竞业协议或禁止协议 |
| isIntellectualProperty | 是否有知识产权 |
| isOtherInvestJob | 是否有在其他单位投资或任职 |
| isCompanyBusiness | 是否与公司有经济业务往来 |
| isPoorHousehold | 是否建卡贫困户 |
| isRelativeSameBusiness | 本人或直系亲属是否经营与公司相同/类似业务 |

**⑥ 证件信息（子表 CertInfo，可多条）**

| API字段名 | 名称 | 类型 | 必填 |
|-----------|------|------|------|
| certificateNo | 证件号 | 单行文本 | 是 |
| country | 国家 | 单选 | 是 |
| certificateType | 证件类型 | 单选 | 是 |
| certificateExpireDate | 证件到期日期 | 日期 | 是 |
| idCardAddress | 身份证地址 | 单行文本 | 是 |
| isTaxCertificate | 是否报税证件 | 单选 | 是 |

**⑦ 语言能力（子表 Language，可多条）**

| API字段名 | 名称 | 类型 | 必填 |
|-----------|------|------|------|
| languageType | 语种 | 单选 | 是 |
| languageLevel | 语言水平 | 单选 | 是 |
| languageCert | 获得的资格证书 | 单行文本 | 否 |
| languageCertExpire | 证件到期日期 | 日期 | 否 |
| isFirstLanguage | 是否第一外语 | 单选 | 是 |

**⑧ 正规教育（子表 Education，可多条）**

| API字段名 | 名称 | 类型 | 必填 |
|-----------|------|------|------|
| eduStartDate | 起始日期 | 日期 | 是 |
| eduEndDate | 终止日期 | 日期 | 是 |
| eduNature | 学历性质 | 单选 | 是 |
| education | 学历 | 单选 | 是 |
| schoolName | 学校 | 单行文本 | 是 |
| major | 专业 | 单行文本 | 是 |
| eduCertNo | 证书编号 | 单行文本 | 否 |
| degree | 学位 | 单选 | 否 |
| studyStatus | 修业状态 | 单选 | 否 |
| getCertificate | 所获证书 | 单选 | 是 |
| isHighestEdu | 是否最高学历 | 单选 | 是 |

**⑨ 过往工作经历（子表 WorkExperience，可多条）**

| API字段名 | 名称 | 类型 | 必填 |
|-----------|------|------|------|
| workExpStartDate | 开始日期 | 日期 | 是 |
| workExpEndDate | 结束日期 | 日期 | 是 |
| workCompany | 工作单位 | 文本 | 是 |
| workDept | 部门 | 文本 | 是 |
| workPost | 职位 | 文本 | 是 |
| workSalary | 薪金 | 文本 | 是 |
| witnessName | 证明人 | 文本 | 是 |
| witnessPhone | 联系电话 | 文本 | 是 |
| resignReason | 离职原因 | 文本 | 是 |
| isInnerWork | 是否公司内部工作 | 单选 | 是 |

**⑩ 家庭成员及社会关系（子表 FamilyRelation，可多条）**

| API字段名 | 名称 | 类型 | 必填 |
|-----------|------|------|------|
| relationType | 关系类型 | 单选 | 是 |
| familyGender | 性别 | 列表 | 是 |
| familyName | 亲属姓名 | 单行文本 | 是 |
| familyPhone | 联系电话 | 单行文本 | 是 |
| familyCompany | 工作单位 | 单行文本 | 是 |
| familyPost | 岗位 | 单行文本 | 是 |
| familyAddress | 现居住地 | 单行文本 | 是 |
| familyRemark | 备注 | 多行文本 | 否 |
| isCompanyStaff | 是否公司人员 | 单选 | 是 |
| isEmergencyContact | 紧急联系人标识 | 单选 | 否 |

**⑪ 资格证信息（子表 Certificate，可多条）**

| API字段名 | 名称 | 类型 | 必填 |
|-----------|------|------|------|
| certEffectDate | 生效日期 | 日期 | 是 |
| certExpireDate | 到期日期 | 日期 | 是 |
| certType | 类型 | 单选 | 是 |
| certName | 证书名称 | 单行文本 | 是 |
| certLevel | 等级 | 单选 | 否 |
| certMajor | 专业方向 | 单行文本 | 否 |
| certIssueOrg | 发证单位 | 单行文本 | 否 |

**⑫ 工作许可及签证信息（子表 VisaPermit，外籍适用，可多条）**

| API字段名 | 名称 | 类型 | 必填 |
|-----------|------|------|------|
| visaIssueDate | 颁布日期 | 日期 | 是 |
| visaExpireDate | 证件到期日期 | 日期 | 是 |
| visaType | 证件类型 | 列表 | 是 |
| visaNo | 证件号 | 单行文本 | 是 |

**⑬ 入职附件（文件字段，走附件代理接口，不走 fieldDataList）**

每类附件对应 ServiceGo 工单上的一个**文件字段**（fieldTypeName = `附件` / `图片`），上传走附件三件套，不进 `fieldDataList`。下表 apiName 为 `GET /api/v1/fields` 实际返回值（2026-05-31 查询确认）。

| 序号 | 附件名称 | 对应字段 apiName | 字段类型 | 容量 | 流程 | 上传规则 |
|------|----------|------------------|----------|------|------|----------|
| 1 | 个人简历 | personalResume | 附件 | 单图 | 入职预约 | 选传 |
| 2 | 体检报告 | physicalExamReport | 附件 | 单图 | 入职预约 | 必传 |
| 3 | 员工照片 | staffPhoto | **图片** | 单图 | 入职预约 | 必传，1寸照，JPEG，< 2MB，推荐 180×240 |
| 4 | 个人有效身份证（正面 + 背面） | validIdCard | 附件 | **多图** | 入职登记 | 必传，正反面 2 张均传到此字段 |
| 5 | 毕业证/电子学历注册备案表 | diplomaRecordForm | 附件 | 单图 | 入职登记 | 选传 |
| 6 | 学位证 | degreeCertificate | 附件 | 单图 | 入职登记 | 选传 |
| 7 | 学生证 | studentCard | 附件 | 单图 | 入职登记 | 选传（仅实习生） |
| 8 | 离职证明/解除劳动合同通知书 | dimissionCert | 附件 | 单图 | 入职登记 | 选传（有工作经历者） |
| 9 | 工资银行卡 | salaryBankCard | 附件 | 单图 | 入职登记 | 选传 |

> **字段差异说明：**
>
> **已确认（结论生效）：**
> - **#3 员工照片**：工单存在 `staffPhoto`（图片类型）和 `staffPhotobak`（附件类型，带 bak 后缀）两个字段。**确认采用 `staffPhoto`**，`staffPhotobak` 不使用（Q12 已关闭）。
> - **资料上传真实性承诺**：工单存在 `dataUploadCommitmentBAK`（带 BAK 后缀，废弃字段）和 `dataUploadCommitment` 两个字段。**确认采用 `dataUploadCommitment`**，`dataUploadCommitmentBAK` 不使用（Q13 已关闭）。
> - **#4 身份证正反面**：工单 `validIdCard`（附件类型）的 `attachment.attachmentList` 为**数组，支持多张附件**。**确认正反面 2 张均上传到 `validIdCard`**，不新建背面字段（Q11 已关闭）。因此 `validIdCard` 为**多图字段**，附件机制需区别于单图字段处理（见 3.4.2）。

> **定位说明（重要）**：本入职业务平台的附件上传为**独立实现**，不依赖、不复用任何现有项目代码。项目历史上存在的 `PhotoController` / `ServiceGoClient`（单字段照片上传，写死 `validIdCard`）仅作为 **ServiceGo 附件对接模式的参考蓝本**，不构成运行时依赖。本平台据此从零设计一套支持「多附件字段」的上传/回显接口，`field` 参数从设计之初即为必填项，不存在「改造老接口」一说。详见 3.4.2。

#### 3.4.2 附件上传机制（本平台独立实现，参考 ServiceGo 对接模式）

附件不进入 `fieldDataList`，由本平台独立设计的代理接口处理。整体覆盖「**页面打开回显已存附件 → 前端校验 → 后端代理上传 → 回显**」完整生命周期，后端对接 ServiceGo 附件四件套（查列表 / 上传 / 删除 / 下载）。

> **字段容量分类（关键设计前提）**：ServiceGo 文件字段的 `attachment.attachmentList` 是**数组**，原生支持多张附件。本平台据此把 **9 个附件字段**（原 10 类材料中身份证正反面合并到 `validIdCard` 一个字段）分两种处理：
> - **单图字段**（personalResume、physicalExamReport、staffPhoto、diplomaRecordForm、degreeCertificate、studentCard、dimissionCert、salaryBankCard 共 8 类）：业务上只保留 1 张。回显取该字段唯一/最新一张；重传 = 「先清空该字段旧图再传新图」（上传即替换）。
> - **多图字段**（`validIdCard`，身份证正反面 2 张共存）：业务上保留多张。回显取该字段**全部**附件列表；上传为「**追加**」一张，不清空已有；删除按 `docId` **精确删单张**，不整体清空。

> **完整生命周期（重要）**：
> 1. **页面打开**：根据工单系统中该字段已保存的附件回显已上传图片（调回显接口，见 (2)）。单图字段显示 1 张，多图字段显示全部。
> 2. **首次上传**：该字段无附件 → 前端校验通过 → 调上传接口。
> 3. **重新上传 / 增删**：
>    - **单图字段**：换图 = 先删旧（清空该字段）再传新，对外即「上传即替换」。
>    - **多图字段**（validIdCard）：再传一张 = **追加**（不动已有）；删某一张 = 按 `docId` 精确删除该张。

> **可参考的 ServiceGo 对接模式（来自蓝本，非依赖）**：① 代理模式（前端经本平台后端中转，不直连 ServiceGo，解决签名+跨域+下载鉴权）；② 四件套流程（查附件列表 GET `/v1/fileField/attachments` → 上传 POST `/api/v1/fileField/attachments` → 删除 PUT `/v1/fileField/attachments/remove` → 下载回显）；③ 签名规则 `SHA-256(email + "&" + apiToken + "&" + timestamp)`；④ 附件读取从 `fieldDataList[].attachment.attachmentList`（图片类回退 `picture.attachmentList`）取最新 `docAddress`，或从查列表接口取 `data[].docId` / `downloadAddress`。

**(1) 上传接口 `POST /api/entry/attachment/upload`（multipart/form-data）**

> 本平台独立设计的接口。接口名仅为约定，最终以实现为准；从设计之初即支持多附件字段（通过 `field` 参数区分各类附件），无「写死单字段」的历史包袱。

| 参数 | 必填 | 说明 |
|------|------|------|
| file | 是 | 附件文件（MultipartFile） |
| phone | 是 | 手机号，后端据此定位入职工单 dataId |
| field | 是 | 目标附件字段 apiName（如 `validIdCard`、`staffPhoto`），区分各类附件 |
| replace | 否 | 是否替换该字段已有附件。单图字段默认 `true`（上传即替换）；多图字段（validIdCard）传 `false` 表示追加。缺省时后端按字段容量分类自动决定 |

后端处理流程（参考蓝本的对接套路，本平台独立实现）：
1. 校验 file、phone、field 非空。
2. `queryDataIdByPhone(entryWorkOrder, "personalPhone", phone)` → 拿到工单 dataId（GET `/api/v1/data`，uniqueFieldApiName = personalPhone）。
3. **按字段容量分类决定是否清旧**：
   - **单图字段**（replace=true）：查该字段附件列表（GET ServiceGo `/v1/fileField/attachments`），若已存在附件先调删除接口（见 (3)）清空，再上传，避免叠加。
   - **多图字段** `validIdCard`（replace=false）：**不清空**，直接追加上传（正反面共存）。
4. `uploadAttachment(dataId, objectApiName, field, fileName, bytes, contentType)` → POST ServiceGo `/api/v1/fileField/attachments`（multipart，form 含 objectApiName / dataId / fieldApiName / file）。
5. 返回：`{ success, dataId, fieldApiName, previewUrl }`（多图字段可返回 `previewUrls` 数组或新增一张的地址），`previewUrl = /api/entry/attachment?dataId=X&field=Y&ts=<时间戳>`（带 ts 防前端缓存旧图）。

> 关于「单图先删后传」的归属：步骤 3 单图分支可由后端在 upload 接口内**自动完成**（推荐，前端无感，单次调用即「替换」语义）；也可由前端先显式调删除接口 (3) 再调上传。本平台默认采用**后端自动清理**方案。多图字段（validIdCard）不走清空逻辑，按追加处理。

**(2) 回显接口 `GET /api/entry/attachment`**

> **页面打开即调用**：资料填写页加载时，对每个附件字段调一次本接口回显已存图片。单图字段显示 1 张，多图字段（validIdCard）显示全部。这是附件生命周期的第一步。

本接口有两种语义，按字段容量使用：

**(2a) 取附件列表（元数据）`GET /api/entry/attachment/list`**

返回该字段下所有附件的元数据，多图字段用它拿到每张的 `docId` 与代理预览地址。

| 参数 | 必填 | 说明 |
|------|------|------|
| dataId | 是 | 工单 ID |
| field | 是 | 附件字段 apiName |

返回：`{ field, items: [ { docId, name, size, previewUrl } ] }`，其中 `previewUrl = /api/entry/attachment?dataId=X&field=Y&docId=Z&ts=...`。后端数据源为 ServiceGo GET `/v1/fileField/attachments`（取 `data[].docId / name / size`）或工单 `attachment.attachmentList`。

**(2b) 取单张图片（流式代理）`GET /api/entry/attachment`**

| 参数 | 必填 | 说明 |
|------|------|------|
| dataId | 是 | 工单 ID |
| field | 是 | 附件字段 apiName |
| docId | 否 | 指定取哪一张；不传时单图字段取唯一/最新一张，多图字段取最新一张 |

后端从 `attachment.attachmentList`（图片类字段回退到 `picture.attachmentList`）按 `docId` 定位（或取最新一张）拿到 `docAddress`，经 `signDownloadUrl` 补签名（email + timestamp + sign，SHA-256）后**流式代理回传**，解决跨域与下载鉴权。返回头 `Cache-Control: no-store`。该字段无附件时返回 404 / 空，前端按「未上传」处理。

> **单图字段**：前端直接用 (2b)（不带 docId）即可回显唯一一张，无需调 (2a)。
> **多图字段**（validIdCard）：前端先调 (2a) 拿列表，再对每个 `previewUrl`（带 docId）渲染缩略图。

> 设计要点：附件下载必须经后端代理加签，前端不能直连 ServiceGo 存储地址（无签名会被拒、且跨域）。

**(3) 删除附件接口 `DELETE /api/entry/attachment`（本平台独立实现，转发 ServiceGo 删除接口）**

> 两种用途：① 单图字段换图前清空旧图；② 多图字段（validIdCard）删掉指定的某一张。本接口封装 ServiceGo 的 PUT `/v1/fileField/attachments/remove`（注意路径前缀比其它接口少 `/api`）。

本平台对外接口参数：

| 参数 | 必填 | 说明 |
|------|------|------|
| dataId | 是 | 工单 ID |
| field | 是 | 附件字段 apiName |
| docIds | 否 | 要删除的附件 docId 数组。**多图字段删单张时必传**（精确删某张）；不传则清空该字段全部附件（单图换图用） |

后端转发到 ServiceGo `PUT /v1/fileField/attachments/remove` 的参数：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| dataId | 整型 | 是 | 记录 ID |
| objectApiName | 字符串 | 是 | = `entryWorkOrder` |
| fieldApiName | 字符串 | 是 | 附件字段 apiName |
| isClear | 整型 | 是 | 是否清空，0=否 1=是 |
| docIds | 字符串数组 | 否 | 要删的 docId；`isClear=0` 时数组必须有元素 |

映射规则：
- **单图换图 / 整体清空**：`isClear=1`，无需 docIds（最简单）。
- **多图删单张**（validIdCard 删正面或背面）：`isClear=0` + `docIds=[要删的那张]`。docId 从 (2a) 列表接口取得。

**(4) 前端上传校验规则（参考批量上传设计）**

针对图片类附件（尤其员工照片），前端在上传前做本地校验，状态分 `pass / warn / fail`，仅 `fail` 阻断上传：

| # | 规则 | 校验方式 | 失败处理 |
|---|------|----------|----------|
| 1 | JPEG 格式 | 文件头魔数 `FF D8 FF`（前 3 字节） | fail |
| 2 | 非 zip / PGP | 黑名单魔数 `50 4B 03 04`(zip)、`99 01`(PGP) | fail |
| 3 | 大小 ≤ 2MB | `file.size <= 2*1024*1024` | fail |
| 4 | 推荐 ≤ 100K | `file.size <= 100*1024` | warn（"建议 ≤100K"） |
| 5 | 尺寸 180×240 | `Image.naturalWidth/Height` | warn（"推荐 180×240"） |

> 注：魔数/尺寸校验主要面向员工照片（1 寸照）。身份证、证书等扫描件可放宽尺寸校验，但格式与大小（≤2MB）规则通用。

**(5) 多附件上传策略**

- **页面打开**：单图字段调 `GET /api/entry/attachment`（见 (2b)）回显 1 张；多图字段（validIdCard）调 `GET /api/entry/attachment/list`（见 (2a)）回显全部。
- 每类附件独立调用 `POST /api/entry/attachment/upload`（带各自 field），互不影响，单个失败可单独重试。
- **单图字段重传**：上传接口内部先清旧再传（后端自动，见 (1) 步骤 3），对外即「上传即替换」；前端也可显式先调 `DELETE` 再传。
- **多图字段**（validIdCard 正反面）：每张独立追加上传（`replace=false`），互不覆盖；删某一张调 `DELETE`（带该张 `docId`）。上传成功后刷新列表回显。
- 上传成功后用返回的 `previewUrl`（带 ts）即时回显，单图覆盖旧图、多图追加一张。
- 批量/多图场景并发上传建议并发数 3，避免 ServiceGo 端压力；前端可用 SHA-256 + localStorage 做客户端去重（"已上传过，跳过"）。
- 弱网下未上传成功的附件保留在列表，恢复后重试，不影响已填写的文本字段。

#### 3.4.3 条件联动规则

| 触发字段 | 条件 | 联动 |
|----------|------|------|
| nationality 国籍 | = 中国 | nation 民族、nativePlace 籍贯 变必填 |
| maritalStatus 婚姻状况 | = 已婚 | 展示并要求 marryDate 结婚日期、配偶信息 |
| politicalStatus 政治面貌 | = 中共党员 | 展示 partyJoinDate 入党时间 |
| bankAccount 银行账号 | 输入卡号 | 自动带出 salaryBank 发薪银行 |
| recruitChannel 招聘渠道 | = 内部推荐 | 内部推荐人 必填 |
| 职员状态 | = 试用 | 试用期（月）必填 |

#### 3.4.4 三种操作模式

| 模式 | 行为 | 工单状态变化 | 校验 |
|------|------|--------------|------|
| 录入 | 前端填写，未提交后端 | 无 | 实时格式校验 |
| 暂存 | 调更新接口存数据 | **不改状态** | 仅格式校验，不校验必填 |
| 提交 | 存数据 + 弹真实性承诺 | 承诺签署后 → 待审核 | **全量必填 + 条件 + 附件**校验 |

**业务规则补充**：
- 提交前必须通过 122 字段全部校验规则 + 必传附件检查，否则提交按钮置灰并定位首个错误字段。
- 文本字段走 `fieldDataList`（PUT 更新工单）；附件走附件代理接口（见 3.4.2），两者分别提交。
- 移动端与 Web 端均需适配，分步引导填写。
- 子表（教育/工作/家庭等）支持「增加一条 / 删除」动态行。

**关联接口**：
- 文本字段写工单：I020-04-01（PUT `/api/v1/data`，更新 `fieldDataList`）。
- 附件上传：`POST /api/entry/attachment/upload`（带 field，本平台独立实现），后端转 ServiceGo `/api/v1/fileField/attachments`。
- 附件回显：`GET /api/entry/attachment?dataId&field`（本平台独立实现，后端加签代理下载；**页面打开即调用回显已存图片**）。
- 附件删除：`DELETE /api/entry/attachment`（本平台独立实现，转 ServiceGo PUT `/v1/fileField/attachments/remove`；**重新上传前删旧图**）。
- 字段元数据 + 选项 + 权限：`GET /api/v1/fields?objectApiName=entryWorkOrder`。
- 工单数据回填：按 personalPhone 查工单（GET `/api/v1/data`）。

**异常处理**：
- 弱网下支持离线暂存，恢复后自动同步。
- 附件上传失败单独重试（按 field 独立），不影响已填文本字段。
- 上传到 ServiceGo 失败时后端返回 `{success:false, message}`，前端按字段提示并允许重试。
- 提交并发：同一工单加乐观锁，防止 HR 同时修改覆盖。

**验收标准**：所有下拉选项来自 optionList；条件联动正确；暂存不校验必填、提交全量校验；必传附件齐全才可提交。

---

### 3.5 RS020105 真实性承诺签署

**功能描述**：提交入职资料时弹出《入职承诺书》，员工签署承诺所填信息真实有效，签署后不可撤销，触发 AI 审核。

**前置条件**：资料校验通过，点击「提交」。

**数据项**：

| 数据项 | 类型       | 必填 | 来源 | 说明 |
|--------|----------|------|------|------|
| 承诺书内容 | markdown | 是 | 合同模块-模板管理（类型=入职承诺书） | 按管控单位/用工单位匹配模板 |
| 确认状态 | 布尔       | 是 | 员工操作 | 已确认/未确认 |
| 确认时间 | 时间戳      | 是 | 系统生成 | |
| 工单状态 | 列表       | 是 | 系统 | 提交后 入职登记 → 待审核 |
| 入职任务-真实性承诺 | 列表       | 是 | 系统 | 签署后置「完成」 |

**业务规则**：
1. 根据工单「管控单位、用工单位」从配置表匹配《入职承诺书》模板，markdown 渲染展示。
2. 员工确认签署 → 生成《资料上传真实性承诺》附件存工单（附件字段 apiName = `dataUploadCommitment`，**非** `dataUploadCommitmentBAK`，后者已弃用）→ 工单状态置「待审核」→ 触发 AI 审核异步任务。
3. 签署不可撤销；签署弹窗需二次确认，未签署不可关闭提交。
4. 未签真实性承诺**不允许提交**入职资料。

**关联接口**：I020-05-01（签署状态写工单）；模板取自合同模块模板管理。

**异常处理**：模板取不到 → 提示「承诺书加载失败」，禁止提交，记录告警。

**验收标准**：模板按单位正确匹配；签署后状态流转与附件生成正确；未签不可提交。

---

### 3.6 RS020107 入职信息 AI 审核

**功能描述**：员工提交后异步发起 AI 审核，调用 Dify 视觉模型识别身份证并与工单字段比对，结果回写工单。

**前置条件**：工单状态 = 待审核，已上传身份证附件。

**数据项**：

| 数据项 | 类型 | 长度 | 必填 | 来源 | 说明 |
|--------|------|------|------|------|------|
| 审核结果 | 枚举 | — | 是 | AI 模块 | 通过 / 错误 |
| 异常字段列表 | 列表 | — | 条件 | AI 模块 | 异常时必填 |
| 异常描述 | 文本 | 500 | 条件 | AI 模块 | 异常原因 |

**业务规则**：
1. 提交触发异步任务（走消息队列），不阻塞员工端。
2. 调用大模型提供的 AI 审核接口（本系统只传图片/数据并接收结果），比对项：身份证姓名↔chineseName、证件号↔certificateNo、有效期↔certificateExpireDate 等。**对方接口未提供，先后端 Mock（返回模拟审核结果），待对方提供后替换为真实接口（Q3）。**
3. 审核结果（通过/错误）+ 异常字段 + 异常描述 回写工单，供 HR 在 PC 端查看。
4. 本期仅身份证识别；工作经历、教育、银行卡为后续扩展。

**关联接口**：I020-07-01（数据推送 AI；AI 结果写工单）。

**异常处理**：Dify 超时（30s）熔断；失败进重试队列（最多 3 次，间隔递增）；最终失败 → 工单标「待人工审核」，**降级为纯人工审核**，不阻断流程。

---

### 3.7 RS060101 二维码签到

**功能描述**：员工到达共享中心，用 SAI 2.0（飞书）扫描现场易拉宝二维码完成入职签到。

**前置条件**：工单状态 = 待签到（审核通过）。

**数据项**：

| 数据项 | 类型 | 必填 | 来源 | 说明                              |
|--------|------|------|------|---------------------------------|
| 签到二维码 | 二维码 | 是 | 现场易拉宝 | 本系统生成一个二维码页面展示，可以打印之后交给对方用于扫描签到 |
| 签到状态 | 枚举 | 是 | 系统 | 已签到 / 未签到                       |
| 签到时间 | 时间戳 | 是 | 系统生成 | 作为劳动关系起始日佐证之一                   |

**业务规则**：
1. 飞书扫码跳转移动端签到确认页，确认后写工单签到状态 + 签到时间。
2. 重复扫码幂等：已签到则提示「您已签到」，不重复写时间。
3. 签到时间作为劳动关系起始日佐证之一，需精确到秒。
4. 签到的二维码绑定签到时间（比如说 2026-01-13,2026-01-14,2026-01-15），扫描的时间不在这个时间段则提示活动未开始或已结束。

**关联接口**：I060-01-01（飞书扫码跳转移动端）、I060-01-02（签到状态更新工单）。

**异常处理**：二维码过期 → 提示联系现场工作人员重新生成。

**验收标准**：扫码正确写入签到时间/状态；重复扫码幂等。

---

### 3.8 RS090102 入职满意度评价

**功能描述**：入职完成后用户会收到其他系统发给用户一个链接，员工点击 H5 链接进入本系统的一个评价页面，进行满意度评价，结果同步工单。

**前置条件**：入职经办人点击「入职完成」。

**数据项**：

| 数据项 | 类型 | 长度 | 必填 | 来源 | 说明 |
|--------|------|------|------|------|------|
| 满意度问题1 | 评分 | 1-5 | 是 | 员工 | 对本次入职服务是否满意 |
| 满意度问题2 | 评分 | 1-5 | 是 | 员工 | 对本次入职流程是否满意 |
| 开放性评价 | 文本 | 1000 | 否 | 员工 | 对入职过程的评价内容 |

**业务规则**：
1. 点击「入职完成」→ 系统自动发满意度评价短信（含 H5 链接，免登 token）。
2. 员工进入 H5 填写 2 道评分（1-5）+ 开放评价，提交后同步对应工单字段。
3. 每个工单仅可评价一次；重复进入展示已评价结果。

**关联接口**：入职工单接口有对应的满意度评价字段，将结果更新进去就好了。

**异常处理**：评价提交失败 → 重试，保留已填内容。

**验收标准**：入职完成自动触发短信；评分与开放评价正确同步工单；不可重复评价。

---

## 4. 接口需求

### 4.1 接口清单

**I020 移动端交互（8）**

| 编号 | 功能 | 消费方 | 提供方 | 备注 |
|------|------|--------|--------|------|
| I020-01-01 | 登录移动端（工单查询 + 短信记录） | 移动端 | 工单系统 | 手机号+状态查工单 |
| I020-02-01 | 隐私声明（是否显示/显示内容） | 移动端 | DPO | DPO 开发中 |
| I020-02-01 | 隐私声明阅读状态写工单 | 移动端 | 工单系统 | |
| I020-04-01 | 提交入职资料（员工信息写工单） | 移动端 | 工单系统 | |
| I020-05-01 | 真实性承诺签署状态写工单 | 移动端 | 工单系统 | |
| I020-06-01 | 在线客服接入 | 移动端 | 工单系统 | 接入文档 |
| I020-07-01 | 数据推送 AI 审核 | 移动端 | AI 审核模块 | |
| I020-07-01 | AI 审核结果写工单 | AI 审核模块 | 工单系统 | |

**I060 二维码签到（2）**：飞书扫码跳转移动端 / 签到状态更新工单。
**I980 终止提醒（1）**：工单系统 → 飞书提醒招聘经办人。
**I000 其他集成（2）**：AD 域集成（已取消）/ HCM 主数据同步（定时拉取）。

### 4.2 工单系统标准接口（ServiceGo / Udesk）

| 接口编码 | 名称 | 用途 | 本项目对应 |
|----------|------|------|------------|
| SG-API-001 | 创建对象记录 | 建工单 | 工单创建 |
| SG-API-002 | 更新对象记录 | 按 ID/唯一字段更新 | 暂存、提交、状态回写 |
| SG-API-003 | 查询单条记录 | 详情 | 登录查工单 |
| SG-API-004 | 文件图片字段上传 | 附件上传 | `POST /api/entry/attachment/upload`（本平台独立实现） |
| SG-API-005 | 文件字段列表 | 附件清单（取 docId/下载地址，供回显与删除定位） | 后端内部调用（回显/删除前查列表） |
| SG-API-006 | 删除附件 | 删/清空（重新上传前删旧图） | `DELETE /api/entry/attachment`（本平台独立实现） |
| SG-API-007 | 外部系统数据推送 | 主动推送 | 同步外部系统 |
| SG-API-008 | 查询字段列表 | 字段元数据 | `GET /api/v1/fields` |
| SG-API-009 | 创建字段选项值 | 选项维护 | 配置 |
| SG-API-0010 | 对象搜索查询 | 多条件搜索 | 按手机号+状态搜工单 |

**鉴权约定**（来自项目 CLAUDE.md）：URL 拼 `email + timestamp + sign`，签名用 **SHA-256**（非文档所写 SHA-1）；搜索接口 `filterId` 必填且需后台预配（**入职工单 entryWorkOrder 已配 `filterId = 241586`**）；`judgeStrategy` 1=AND / 2=OR。

### 4.3 集成系统总表

| 编码 | 系统 | 场景 | 优先级 |
|------|------|------|--------|
| INT_SM_01 | 短信平台（赛力斯短信网关，`SmsService`，见 §3.1 短信平台说明） | 验证码、通知 | P0 |
| INT_ES_01 | 慧博电子签 | 签署/用印/回写 | P0（基础已实现） |
| INT_EM_01 | 邮件系统 | 入职邮件 | P1 |
| INT_FS_01 | 飞书 | 机器人消息/扫码 | P1 |
| — | DPO | 隐私声明 | P1 |
| — | Dify | AI 审核 | P1 |

---

## 5. 数据需求

### 5.1 数据实体总览（9 个）

| 序号 | 实体 | apiName | 字段数 | 关系 |
|------|------|---------|--------|------|
| 1 | 入职工单 | entryWorkOrder | 111 | 主表 |
| 2 | 证件信息 | CertInfo | 8 | 子表（一对多） |
| 3 | 语言能力 | Language | 7 | 子表 |
| 4 | 正规教育 | Education | 13 | 子表 |
| 5 | 过往工作经历 | WorkExperience | 12 | 子表 |
| 6 | 家庭成员及社会关系 | FamilyRelation | 12 | 子表 |
| 7 | 资格证信息 | Certificate | 9 | 子表 |
| 8 | 工作许可及签证信息 | VisaPermit | 6 | 子表 |
| 9 | 附件清单 | — | 28 | 附件 |

**数据流转**：职位申请表 → 录用审批表 → 入职工单。
**关联关系**：
- 入职工单 ↔ 7 子表：经 `relatedWorkOrder` 一对多。
- 入职工单大部分字段由录用审批表同步。

> 字段数说明：上表「字段数」为实体在主数据清单中的总字段数（含 `relatedWorkOrder` FK、`记录ID` PK 等系统字段）；§3.4.1 资料填写表单列出的为员工**需填写的业务字段**，故每个子表的可填字段数比总字段数少 1~2 个系统字段（如 Education 总 13、可填 11）。两者不矛盾。

### 5.2 入职工单字段分布（111 字段 / 8 类）

| 信息类型 | 字段数 | 必填数 | 说明 |
|----------|--------|--------|------|
| 招聘渠道 | 3 | 2 | 招聘渠道、渠道细分、内部推荐人（内推时必填） |
| 组织岗位信息 | 23 | 21 | 事件、管控单位、岗位、职级、入司日期等，多由录用审批表同步 |
| 个人信息 | 24 | 18 | 姓名、证件、国籍、婚姻等 |
| 银行账号 | 2 | 0 | 卡号自动带出发薪银行 |
| 其他信息 | 15 | 15 | 合规声明类单选 |
| 入职附件信息 | 14 | — | 附件文件 |
| 工单系统字段 | 17 | 5 | 状态、审核、签到、满意度等系统管理字段 |
| 子表引用 | 7 | — | 7 子表关联 |

#### 5.2.1 招聘渠道（#1~#3）

| # | 名称 | 输入类型 | 必填 | 值来源 |
|---|------|----------|------|--------|
| 1 | 招聘渠道 | 列表 | 是 | 录用审批表同步 |
| 2 | 招聘渠道细分 | 列表 | 是 | 录用审批表同步 |
| 3 | 内部推荐人 | 查找员工 | 条件 | 招聘渠道=内部推荐 时必填 |

#### 5.2.2 组织岗位信息（#4~#26，多由录用审批表同步）

| # | 名称 | 输入类型 | 必填 | 值来源 |
|---|------|----------|------|--------|
| 4 | 事件 | 列表 | 是 | 系统按规则填充 |
| 5 | 事件原因 | 列表 | 是 | 系统按规则填充 |
| 6 | 管控单位 | 列表 | 是 | 录用审批表同步 |
| 7 | 用工形式类别 | 列表 | 是 | 录用审批表同步 |
| 8 | 用工形式 | 列表 | 是 | 录用审批表同步 |
| 9 | 组织/部门 | 列表 | 是 | 录用审批表同步 |
| 10 | 组织/部门描述 | 文本 | 是 | 录用审批表同步 |
| 11 | 岗位 | 列表 | 是 | 录用审批表同步 |
| 12 | 入司日期 | 日期 | 是 | 录用审批表同步 |
| 13 | 用人单位 | 列表 | 是 | 录用审批表同步 |
| 14 | 工作地点 | 列表 | 是 | 录用审批表同步 |
| 15 | 派遣公司 | 列表 | 否 | 录用审批表同步 |
| 16 | 直间属性 | 列表 | 是 | 录用审批表同步 |
| 17 | 职级 | 列表 | 是 | 录用审批表同步 |
| 18 | 职等 | 列表 | 是 | 录用审批表同步 |
| 19 | 职级-职等 | 文本 | 是 | 录用审批表同步 |
| 20 | 外单位累计工龄（月） | 数字 | 是 | 录用审批表同步；默认 0 |
| 21 | 用工单位 | 列表 | 是 | 录用审批表同步 |
| 22 | 职员状态 | 列表 | 是 | 为"试用"时试用期必填 |
| 23 | 试用期（月） | 数字 | 条件 | 录用审批表同步 |
| 24 | 试用期结束日期 | 日期 | 否 | 系统计算 = 入司日期 + 试用期（月） |
| 25 | 职族 | 列表 | 否 | 录用审批表同步 |
| 26 | 报到地点 | 列表 | 是 | 录用审批表同步（驱动入职指引动态展示） |

#### 5.2.3 个人信息（#27~#50）

| # | 名称 | 输入类型 | 必填 | 值来源 |
|---|------|----------|------|--------|
| 27 | 员工号 | 文本 | 否 | I9 系统生成 |
| 28 | 中文全名 | 文本 | 是 | 录用审批表同步；须与身份证一致 |
| 29 | 外文全名 | 文本 | 是 | 中文全名自动带拼音，可手工改 |
| 30 | 参加工作日期 | 日期 | 是 | 录用审批表同步 |
| 31 | 个人电话 | 电话 | 是 | 录用审批表同步 |
| 32 | 个人邮箱 | 邮箱 | 是 | 录用审批表同步 |
| 33 | 国籍 | 列表 | 是 | 录用审批表同步 |
| 34 | 性别 | 列表 | 是 | 录用审批表同步 |
| 35 | 出生日期 | 日期 | 是 | 录用审批表同步；须与身份证一致 |
| 36 | 婚姻状况 | 列表 | 是 | =已婚 时配偶信息必填 |
| 37 | 结婚日期 | 日期 | 条件 | 婚姻状况=已婚 时展示 |
| 38 | 民族 | 列表 | 条件 | 国籍=中国 时必填 |
| 39 | 籍贯 | 列表 | 条件 | 国籍=中国 时必填 |
| 40 | 户口性质 | 列表 | 是 | 录用审批表同步 |
| 41 | 户口所在地 | 列表 | 是 | 须与身份证信息一致 |
| 42 | 政治面貌 | 列表 | 是 | 录用审批表同步 |
| 43 | 入党时间 | 日期 | 条件 | 政治面貌=中共党员 时展示 |
| 44 | 驾照类型 | 列表 | 是 | 员工填入 |
| 45 | 健康状况 | 列表 | 是 | 员工填入 |
| 46 | 不健康说明 | 文本 | 否 | 员工填入 |
| 47 | 身高 | 数字 | 否 | 员工填入 |
| 48 | 体重 | 数字 | 否 | 员工填入 |
| 49 | 家庭地址 | 文本 | 是 | 省/市/区下拉 + 街道手输 |
| 50 | 邮政编码 | 数字 | 否 | 员工填入 |

#### 5.2.4 银行账号（#51~#52）

| # | 名称 | 输入类型 | 必填 | 值来源 |
|---|------|----------|------|--------|
| 51 | 银行账号 | 数字 | 否 | 员工填入；按卡号自动带出发薪银行 |
| 52 | 发薪银行 | 列表 | 否 | 系统自动带出 |

#### 5.2.5 其他信息（#53~#67，均员工填入）

| # | 名称 | 输入类型 | 必填 |
|---|------|----------|------|
| 53 | 是否复转或退伍军人 | 单选 | 是 |
| 54 | 是否残疾人 | 单选 | 是 |
| 55 | 是否有犯罪记录 | 单选 | 是 |
| 56 | 是否吸毒史 | 单选 | 是 |
| 57 | 是否有慢性病或重大病史 | 单选 | 是 |
| 58 | 是否签订竞业协议或禁止协议 | 单选 | 是 |
| 59 | 是否有知识产权 | 单选 | 是 |
| 60 | 是否有在其他单位投资或任职 | 单选 | 是 |
| 61 | 是否与公司有经济业务往来 | 单选 | 是 |
| 62 | 是否住宿舍 | 单选 | 是 |
| 63 | 是否建卡贫困户 | 单选 | 是 |
| 64 | 本人或直系亲属是否经营与公司相同/类似业务 | 单选 | 是 |
| 65 | 是否停车 | 单选 | 是 |
| 66 | 是否交通车 | 单选 | 是 |
| 67 | HRBP | 人员选择 | 是 |

> 注：§3.4.1 资料填写表单中「其他信息」按需求 RS020104 列了 11 项校验字段；此处 5.2.5 为入职工单主数据完整清单（15 项），多出 是否住宿舍/是否停车/是否交通车/HRBP 四项。

#### 5.2.6 入职附件信息（#74~#87）

| # | 名称 | 传入流程 | 保存位置 |
|---|------|----------|----------|
| 74 | 《职位申请表》 | 010-入职预约 | 入职工单-附件 |
| 75 | 《面试评估表》 | 入职预约 | 入职工单-附件 |
| 76 | 《背景调查表》 | 入职预约 | 入职工单-附件 |
| 77 | 测评报告 | 入职预约 | 入职工单-附件 |
| 78 | 个人简历 | 入职预约 | 入职工单-附件 |
| 79 | 体检报告 | 入职预约 | 入职工单-附件 |
| 80 | 员工照片 | 入职预约 | 入职工单-附件 |
| 81 | 《资料上传真实性承诺》 | 020-入职登记 | 入职工单-附件 |
| 82 | 个人有效身份证 | 入职登记 | 入职工单-附件 |
| 83 | 毕业证/电子学历注册备案表 | 入职登记 | 入职工单-附件 |
| 84 | 学位证 | 入职登记 | 入职工单-附件 |
| 85 | 学生证 | 入职登记 | 入职工单-附件 |
| 86 | 离职证明/解除劳动合同通知书 | 入职登记 | 入职工单-附件 |
| 87 | 工资银行卡 | 入职登记 | 入职工单-附件 |

#### 5.2.7 工单系统字段（#88~#104）

| # | 名称 | 必填 | 值来源 |
|---|------|------|--------|
| 88 | 工单状态 orderStatus | 是 | 系统管理 |
| 90 | 入职预约完成 | 否 | 系统自动 |
| 91 | 发送入职邮件 | 否 | 系统自动 |
| 92 | 发送入职短信 | 否 | 系统自动 |
| 93 | 入职工单审核 | 否 | 入职经办人 |
| 94 | 入职工单审核通过 | 否 | 入职经办人 |
| 95 | 员工入职时间 | 是 | 系统自动 |
| 96 | 工卡激活充值 | 否 | 入职经办人 |
| 97 | 工单签到复审 | 否 | 入职经办人 |
| 98 | 是否签到 | 是 | 系统自动 |
| 99 | 照片审核 | 否 | 入职经办人 |
| 100 | 是否提交照片 | 是 | 入职经办人 |
| 101 | 发送部门报到通知 | 否 | 入职经办人 |
| 102 | 工牌移交完成 | 是 | 入职经办人 |
| 103 | 入职满意度评价 | 否 | 员工评价 |
| 104 | 记录ID | — | PK |

> 注：字段编号 #74~#104 沿用主数据清单的稳定编号；缺号（如 #68~#73、#89）为主数据清单中未纳入本期范围或归属其它分组的字段，属正常缺口。

### 5.3 附件清单（28 项，按用工形式）

**通用必传**：职位申请表、面试评估表、背景调查表、测评报告、个人简历、体检报告、员工照片、资料上传真实性承诺、个人有效身份证、毕业证/学历备案表、工资银行卡、入职承诺书、网络与数据安全责任承诺书。
**按需**：学生证（仅实习生）、离职证明（有工作经历者）、个人健康状况承诺书、劳动合同（全日制）、劳务协议（劳务用工）、实习协议（实习生）、非全日制劳动合同、补充协议（竞业）、保密协议（所有）、廉洁协议（全日制/劳务/派遣）、见习协议、无招聘费用返费承诺书、无竞业限制申明、职业病危害因素告知书。

**附件存储路径**：`file/{tenant_id}/{category}/{yyyyMMdd}/{random_chars}/{original_filename}`（OSS）。

> 原文存疑点（待业务确认）：《资料上传真实性承诺》在入职工单字段表（#81）标「入职工单-附件」，在本节附件清单标「合同工单-附件」，两处不一致，需确认其最终保存位置。本文档暂按入职工单字段表（#81）保留为「入职工单-附件」，落地前请与业务方核实（已登记为待确认事项）。

---

## 6. 非功能性需求

### 6.1 用户界面

| 项 | 要求 |
|----|------|
| PC 端 | Web 浏览器，左侧导航 + 右侧内容区，遵循赛力斯 UI 规范 |
| 移动端 | H5，卡片式布局，分步引导，适合首次使用者 |
| 色彩 | 企业红主色 + 白色背景 |
| 工单列表 | 多维筛选/排序，批量勾选，每页 20 条分页 |
| 表单页 | 标注必填/选填，字段含说明示例，支持暂存分次填写 |
| 消息提醒 | Toast 即时反馈，关键操作弹窗二次确认 |

### 6.2 性能

| 指标 | 要求 |
|------|------|
| 页面加载 | ≤ 2 秒 |
| 表单提交 | ≤ 3 秒 |
| 查询操作 | ≤ 1 秒 |
| 合同生成 | ≤ 10 秒/份 |
| 批量操作(50条) | ≤ 30 秒 |
| 并发 | 日常 100 / 高峰 300 / 峰值 500 人 |

### 6.3 质量属性

| 属性 | 要求 |
|------|------|
| 正确性 | 合同关键条款 100% 准确，122 字段校验覆盖 100%，AI 身份证 ≥99% |
| 可靠性 | 可用性 ≥99.9%（年停机 <8.76h），故障恢复 <1h，RPO <1h |
| 易用性 | 员工无需培训；经办人培训 ≤2 天；活动从 36 优化至 15 |
| 安全性 | RBAC；身份证/手机/银行卡 脱敏 + AES-256；全站 HTTPS(TLS1.2+)；操作日志保留 ≥3 年；等保 2.0 三级 |
| 可扩展性 | 模块化，支持调动/续签/社保/考勤扩展；合同模板可配置 |
| 兼容性 | Chrome/Edge/Safari/Firefox 主流版本；iOS 13+/Android 9+；对接 7 大外部系统 |
| 可移植性 | 容器化部署，不绑定云平台，数据库可迁移 |

### 6.4 软硬件环境

- **服务器**：容器化（Docker/K8s），等保 2.0 三级；关系型数据库（千万级、主从、读写分离）；文件存储（合同加密）；Redis 缓存；消息队列（异步审核/通知）。
- **客户端**：PC 主流浏览器（不支持 IE）；移动端 iOS13+/Android9+；4G/5G/WiFi，弱网基本可用；现场设备（签到易拉宝、工卡读卡器、扫描仪）。

### 6.5 故障处理

| 故障 | 策略 |
|------|------|
| 服务器 | 双节点，主备自动切换；DB 主从切换 ≤5 分钟；周全量 + 日增量备份，异地存储 |
| 系统运行 | 详细错误日志（时间/模块/错误码/堆栈）；邮件短信失败重试 3 次；AI 不可用降级人工 |
| 用户操作 | 表单实时校验；关键操作二次确认；限时撤销；在线客服入口 |
| 外部系统 | 接口超时 30s 熔断；失败进重试队列（≤3 次，间隔递增）；电子签不可用切线下 |
| 网络 | 移动端离线暂存，恢复自动同步；弱网基本功能可用 |
| 数据 | 异常记录日志 + 通知管理员；数据修正走审批；定期一致性检查 |

---

## 7. 约束与假设

### 7.1 技术约束
- 后端复用 Spring Boot（8283），前端复用 Vite（5173），不引入新框架。
- 工单字段定义一律通过 `GET /api/v1/fields` 动态获取，**下拉选项不可硬编码**，按 apiName 建 Map 缓存复用。
- ServiceGo 签名用 SHA-256；搜索接口 filterId 必填需后台预配（入职工单已配 `241586`）。
- 电子签免登链接、企业盖章、结束签署、回调已实现，本期在此基础集成。
- 附件遵循 OSS 路径模板，合同文件加密存储。

### 7.2 业务约束
- 入职指引、隐私声明内容由外部接口提供，本系统只展示不维护。
- 满意度评价为可选，不影响入职完成终态。

### 7.3 待确认事项（风险登记）

| 编号 | 事项 | 影响功能 | 阻塞级 | 负责方 | 状态 |
|------|------|----------|--------|--------|------|
| Q1 | DPO 隐私协议接口文档与联调档期 | RS020102 | 高 | DPO 团队 | open（先 Mock） |
| Q2 | 入职指引接口文档（含 guide.html 字段映射） | RS020103 | 高 | 对接方 | open（先 Mock） |
| Q3 | Dify 视觉模型接口规范、调用配额、比对规则 | RS020107 | 高 | AI 团队 | **deferred：对方接口未提供，先后端 Mock，待提供后替换为真实接口** |
| Q4 | ServiceGo 入职工单 filterId 配置 | 登录/搜索 | 高 | ServiceGo 管理员 | **✅ resolved：已有 filterId = `241586`（见 `EntryWorkOrderSearchByPhoneAndStatusTest`）** |
| Q7 | 入职承诺书模板与「管控单位/用工单位→模板」配置表 | RS020105 | 中 | 合同模块 | open |
| Q8 | 附件单文件大小上限、格式白名单 | RS020104 | 低 | 架构团队 | open（暂按 2MB/JPEG） |
| Q9 | 《资料上传真实性承诺》保存位置（入职工单 vs 合同工单，原文不一致） | RS020104/附件 | 低 | 业务方 | open（暂按入职工单） |
| Q10 | 附件接口的 `field` 参数取值清单：9 个附件字段各自的 apiName 是否都已在 ServiceGo `GET /api/v1/fields` 确认（本平台附件接口设计已含 field 参数，此项仅确认 apiName 取值，见 3.4.1 ⑬） | RS020104/附件 | 中 | ServiceGo 管理员 | open |
| Q11 | 身份证背面字段不存在：工单仅有 `validIdCard`（label=证件照），无独立背面字段。确认：正反面共用 `validIdCard`（多附件）还是新建背面字段 | RS020104/附件 | 高 | ServiceGo 管理员 + 业务方 | **✅ resolved：`validIdCard` 的 attachmentList 支持多张，正反面 2 张均上传到 `validIdCard`，不新建背面字段；validIdCard 按「多图字段」处理（见 3.4.2）** |
| Q12 | 员工照片存在两个字段：`staffPhoto`（图片类型）和 `staffPhotobak`（附件类型）。确认用哪个、bak 是否废弃 | RS020104/附件 | 中 | ServiceGo 管理员 | **✅ resolved：采用 `staffPhoto`，`staffPhotobak` 弃用** |
| Q13 | 资料上传真实性承诺字段为 `dataUploadCommitmentBAK`（带 BAK 后缀），疑似废弃字段，与 Q9 叠加，需明确正式字段位置 | RS020104/附件 | 中 | 业务方 | **✅ resolved：采用 `dataUploadCommitment`，`dataUploadCommitmentBAK` 弃用** |

---

## 8. 里程碑与交付计划

| 阶段 | 周期 | 范围 | 出口标准 |
|------|------|------|----------|
| **Phase 1 核心入职**（P0） | 6 周 | 验证码登录、隐私签署、122 字段资料填写/暂存/提交、真实性承诺、工单状态流转 | 员工可走完登录→提交全流程；工单正确回写 |
| **Phase 2 审核与签到**（P1） | 4 周 | AI 审核（Dify 身份证）、入职指引、飞书扫码签到 | AI 异步审核可用且可降级；签到回写正确 |
| **Phase 3 通知与评价**（P1-P2） | 3 周 | 飞书/短信通知（报到、终止）、满意度评价 H5 | 通知触达闭环；评价同步工单 |
| **Phase 4 上线**（P2） | 2 周 | 500 并发压测、等保三级审计、端到端联调 | 性能/安全达标；全链路联调通过 |

**关键路径依赖**：Phase 1 不依赖外部新接口（登录/资料基础已具备），可立即启动；Phase 2 强依赖 Q1/Q2/Q3 外部接口就绪。

---

## 附录 A：字段控件与缓存示例

```javascript
// 进入工单页拉取一次字段定义，按 apiName 建 Map
const fieldMap = new Map(fieldsData.map(f => [f.apiName, f]));
// 下拉选项从 optionList 取，按 id 升序，传值传 name
const options = (fieldMap.get('recruitChannel')?.optionList ?? [])
  .sort((a, b) => a.id - b.id)
  .map(o => o.name);
// 权限：permissionCode 2=只读 4=可编辑
const editable = fieldMap.get('chineseName')?.permissionCode === 4;
```

## 附录 B：ServiceGo 附件对接参考蓝本（非本平台依赖）

> **说明**：本附录描述的是项目历史上的单字段照片上传实现，**仅作为本入职业务平台对接 ServiceGo 附件的参考蓝本**，帮助理解附件三件套的对接套路。本平台附件功能为**独立实现**（接口见 3.4.2 的 `/api/entry/attachment/*`），与此蓝本无运行时耦合。

蓝本：`PhotoController`（Spring Boot，8283）+ `ServiceGoClient`，对接 ServiceGo 附件三件套。

| 方法 | 路径 | 参数 | 功能 |
|------|------|------|------|
| POST | `/api/upload` | file, phone | 按 personalPhone 查工单 dataId → 上传至附件字段（写死 `validIdCard`，配置项 `servicego.entry-id-card-field`）→ 返回 `{success, dataId, fieldApiName, previewUrl}` |
| GET | `/api/attachment` | dataId, field | 取该字段最新一张附件 docAddress，加签名（SHA-256）后由后端**流式代理**回传，解决跨域与下载鉴权；返回头 `Cache-Control: no-store` |

**可参考的对接套路（ServiceGoClient）**：
- `queryDataIdByPhone(objectApiName, "personalPhone", phone)` → GET `/api/v1/data`（uniqueFieldApiName 定位）。
- `uploadAttachment(dataId, objectApiName, fieldApiName, fileName, bytes, contentType)` → POST `/api/v1/fileField/attachments`（multipart）。
- `latestAttachmentUrl(dataId, objectApiName, fieldApiName)` → 从 `fieldDataList[].attachment.attachmentList`（图片类回退 `picture.attachmentList`）取最新附件地址。
- `signDownloadUrl(url)` → 补 email + timestamp + sign。
- 签名：`sha256(email + "&" + apiToken + "&" + timestamp)`。

> **与本平台的差异**：蓝本只支持单字段（写死 `validIdCard`）；本平台从设计之初即通过 `field` 参数支持多个附件字段（含 validIdCard 多图），是全新独立实现，不是对蓝本的「改造」（详见 3.4.2、待确认事项 Q10）。

## 附录 C：移动端 H5 页面原型清单（`F:\Jay\h5-app\`）

已有一套完整移动端 H5 原型，统一 `css/common.css`（赛力斯企业红主题）+ `js/common.js`，底部 4 Tab 导航（首页/办理/客服/我的）。开发以此为 UI 基线，数据全部走后端接口。

| 页面文件 | 对应功能 | 说明 |
|----------|----------|------|
| index.html | 入口 | 路由入口 |
| login.html | RS020101 登录 | 手机号 + 验证码登录 |
| guide.html | RS020103 入职指引 | 首页：欢迎卡 + 6步流程 + 材料清单 + 报到地点 + 注意事项（详见 3.3.1） |
| info-submit.html | RS020104 资料提交 | 122 字段分组表单填写/暂存/提交（页面体量最大 ~42KB） |
| handle.html | 办理 | 办理进度 Tab 页 |
| service.html | I020-06 在线客服 | 客服 Tab 页 |
| im-guide.html | 飞书安装引导 | 扫码下载飞书，签到准备 |
| profile.html | 我的 | 个人中心 Tab 页 |
| checkin-success.html | RS060101 签到 | 扫码签到成功页 |
| satisfaction.html | RS090102 满意度评价 | 2 道评分 + 开放评价（最近更新 05-28） |

> 注：原型中的隐私声明签署（RS020102）、真实性承诺签署（RS020105）页面待补充或在 info-submit 流程内以弹窗形式实现。

---

*本文档基于《需求规格说明书_入职业务_v1.0》、移动端 H5 原型（`F:\Jay\h5-app\`）及项目技术上下文（CLAUDE.md）整理。需求变更请同步更新版本号与对应章节。*


